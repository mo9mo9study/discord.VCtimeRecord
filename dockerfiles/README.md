# quick start
```
$ docker build -t discord:python_mysql -f dockerfiles/dockerfile_mysql_python .

$ docker images

$ docker run --name test -ti discord:vstimerecord /bin/bash

$ docker exec -ti test /bin/bash

Ctrl + P + Q
```